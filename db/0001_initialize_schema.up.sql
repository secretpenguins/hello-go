CREATE TABlE Posts(PostId INT NOT NULL PRIMARY KEY AUTO_INCREMENT, Title VARCHAR(100) NOT NULL, Content TEXT NOT NULL);
CREATE TABLE Logins(LoginId INT NOT NULL PRIMARY KEY AUTO_INCREMENT, UserName VARCHAR(50) NOT NULL, Password VARCHAR(50) NOT NULL);